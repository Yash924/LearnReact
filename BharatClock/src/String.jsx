import { DEFAULT_BREAKPOINTS } from "react-bootstrap/esm/ThemeProvider"

function String()
{
    return (
        <div className="string">
            <h2>This is the clock that shows the time in Bhaarat at all Times</h2>
        </div>
    )
}

export default String;